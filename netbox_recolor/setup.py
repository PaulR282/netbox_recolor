from setuptools import find_packages, setup

setup(
    name='netbox_recolor',
    version='1.0',
    description='netbox_recolor',
    install_requires=[],
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
)
